# Kallisto 1.1.0

## Goals (in this minor release)

- `kallisto_config.yaml` hoặc `.toml`
- Chia tách rõ hai chế độ qua config flag (`role: sovereign | proxy`)
- State machine: Seal trait + Transit backend
- Encryption barrier
- Module storage backend mới với redb (đã có Hexagonal port)


## Non-goals (later - need to place in roadmap)

- Raft / clustering cho control plane
- Foca gossip cho Data Plane
- Terraform provider cho Control Plane để provisioning

## Visible changes

- config: thêm `role:`, thành bắt buộc
- breaking: file config cũ không parse được nữa

## Work items

1. Config schema tagged-enum → brief 01, ADR-0011
2. Seal trait → brief 02, ADR-0004
3. Barrier + buffer pool → brief 03, ADR-0012

## Nghiệm thu

- cargo test --workspace 100% pass rate
- bench-laptop giữ trong 10% của baseline 1.386ms
- validate reject được config sai role

## Khuyến nghị

Làm proxy mode trước, và để chế độ sovereign lại cho tới khi có người dùng thật hỏi xin nó. 

Nên làm proxy mode trước vì nó không hy sinh gì cả. Nó là con đường ngắn nhất tới một thứ hoàn chỉnh, và cũng là con đường tới chế độ sovereign. Hơn nữa, ~70% code dùng chung: engine, HTTP, cuckoo table, auth, barrier. 

Encryption barrier cần ở cả hai chế độ, chỉ khác chỗ nó bảo vệ: proxy thì bảo vệ RAM, standalone thì bảo vệ đĩa.

# Details

## Configuration format

Xem [ADR-0003: Configuration format](../ADRs/ADR-0003.md).

## Frontend

Xem [TUI vs WebUI in Kallisto](../ADRs/ADR-0004.md).

## Proxy mode

Naughtian Kallisto sẽ được phát triển chế độ vận hành mới: `proxy` mode.

(Giống Kong chạy stateless, nạp sẵn cấu hình và chỉ phục vụ dữ liệu).

### Danh sách tính năng lược giản

| Bỏ | Vì |
|---|---|
| RocksDB + write-behind | dữ liệu không thuộc về mình |
| Mã hoá lúc lưu, unseal, Shamir lên dữ liệu ghi trên đĩa | không còn đĩa để bảo vệ |
| Lịch sử phiên bản, undelete, destroy | chuyển tiếp lên upstream |
| Lease, renewal, revocation machinery | việc của control plane |
| Audit đồng bộ | upstream mới là hồ sơ pháp lý |

### Ưu điểm của proxy mode

- Lược bỏ việc ghi đĩa, biến `SipHash key` trở nên ngẫu nhiên mỗi lần khởi động,
  ngăn chặn hash collison attack.

### Cơ chế gossip cluster

- Control plane publish tin xoá cache cho 1 node, và các node trong cluster sẽ lan truyền tin xoá cache.

- Chống cache invalidate stampede lên upstream khi các data plane đòi secret mới cùng lúc.

#### Requirements

- Thứ lan truyền là lệnh xoá, không phải secret. 
- Không có dữ liệu nhạy cảm nào rời khỏi node. 
- Nhất quán cuối cùng cũng chấp nhận được vì có `hard TTL`.

### Yêu cầu kỹ thuật cho `proxy mode`

#### 1. Token của chính Kallisto với Roots of Trust

Kallisto cần có cơ chế xác thực và phân quyền cho app gọi vào.

Kallisto cần một credential để gọi Vault, và token đó có TTL, cần vòng lặp gia hạn, cần tự auth lại khi hỏng. Hết hạn là mọi cache miss chết. 

Nếu policy của token đó là `secret/*` thì **chiếm được Kallisto = chiếm được toàn bộ kho**. Policy phía upstream cho node phải hẹp, lý tưởng là hợp của những gì pod trên node đó thật sự cần.

#### 2. Chỉ được nạp cache từ upstream. Không bao giờ từ nhận secret từ client.

Nếu ai đó ghi được vào cache, họ thay password database bằng password của họ, và app kết nối vào database của kẻ tấn công (cache poisoning.)

#### 3. Tách hai kiểu hỏng: fail-closed cho phân quyền, fail-open cho fresh state.

- Không xác minh được token (JWKS không với tới được) thì từ chối. Không bao giờ phục vụ cho client chưa xác minh.
- Không làm mới được dữ liệu (upstream chết) thì phục vụ đồ cũ & ghi log.

#### 4. Header nói rõ nguồn gốc

```bash
X-Kallisto-Source: cache
X-Kallisto-Age: 12s
```

Header này giúp app nhạy cảm tự quyết định bỏ qua cache. Và khi có sự cố giờ trả lời được câu hỏi đầu tiên: "giá trị đó cũ bao lâu?"

#### 5. Arena Memory xin cấp phát phải vừa cgroup limit

DaemonSet có memory limit. Arena cấp cố định lúc khởi động có ưu điểm: bạn chết lúc boot chứ không bị OOMKill lúc 3 giờ sáng. 

Nhưng phải tính: **[conflict] 256K entry × kích thước secret trung bình + dung lượng chỉ mục, so với limit của container.** Và ghi công thức đó vào README để người ta biết đặt limit bao nhiêu.

#### 6. Bảo vệ secret path trong log

Đội bảo mật sẽ muốn biết pod nào đọc key nào lúc mấy giờ. Nhưng log đó chứa đường dẫn.

Cách tách: log băm của đường dẫn, hoặc đẩy vào một sink có quyền truy cập riêng, tách khỏi log ứng dụng thông thường.

#### Thứ tự ưu tiên

Nếu phải xếp thứ tự thì 1 và 3 là ưu tiên hàng đầu. 2 là toàn vẹn. 4, 5, 6 là vận hành, làm sau cũng được nhưng làm sớm thì rẻ.

## Lưu trữ

### Cấu hình

Cấu hình của Kallisto là một file YAML.

Đọc lúc khởi động, **[conflict] không bao giờ ghi**. Đó là `std::fs::read`. Không phải việc của database. Kallisto proxy nhận config từ file cục bộ. File đã có sẵn sau restart. Không có gì để cache. (cần atomic naming để replace file config mới, tránh trường hợp config chưa xong bị read).

### Log

Ra `stdout` và `stderr`. journald, Loki, Fluent Bit nhặt.

### Trạng thái cluster SWIM / Foca

SWIM không cần lưu gì cả. Toàn bộ điểm mạnh của gossip membership là nó tự khám phá lại. Node restart thì gia nhập lại và học lại danh sách trong vài giây.

Thứ duy nhất cần bền vững là node ID ổn định — để khi restart nó quay lại như thành viên cũ chứ không xuất hiện như một node mới lạ (làm nhiễu danh sách và tốn một chu kỳ phát hiện chết).

Đó là một UUID trong một file. Hoặc lấy từ `/etc/machine-id`. Không cần DB.

Seed peer list thì là cấu hình, quay về mục trên.

### Yêu cầu

| Thành phần | Mô tả | Lưu trữ |
|---|---|---|
| Cấu hình | File YAML, ConfigMap mount vào pod | file, chỉ đọc |
| Node ID | UUID, dùng cho SWIM/Foca | một file, ghi một lần |
| DEK đã bọc (nếu sau này làm TPM) | một blob, một file |

Cả ba đều là write + rename nguyên tử. Không có truy vấn, không có khoảng, không có transaction, không có concurrency.

### Ưu điểm

1. Không có file DB nghĩa là "không có secret trên đĩa". Kill process là mọi plaintext biến mất. Đây là lợi điểm lớn nhất của proxy mode so với stateful proxy.

2. Bỏ redb khỏi bản proxy nghĩa là binary nhỏ hơn, khởi động nhanh hơn, và cross-compile sang ARM64 cho biên dễ hơn — phù hợp với Helvetilte.

```bash
cargo build --no-default-features      # proxy: không link storage
cargo build --features controlplane    # có redb
```
### Kết luận

proxy dùng file thường. Sovereign dùng `redb`. Sự khác biệt đó là ranh giới giữa hai chế độ.

# Hybrid mode (có thêm control-plane) a.k.a Kallisto Sovereign

*Lưu ý: Định nghĩa về Sovereign đã được cập nhật dựa trên ADR-0006. Sovereign hiện tại dùng để làm bí danh cho chế độ Kallisto Hybrid, trong đó có `control-plane` để điều phối toàn hạm đội proxy.*

## Quy tắc quyết định & Biến thể hoạt động

Kallisto Hybrid (Sovereign) có 2 biến thể tuỳ thuộc vào việc kiến trúc của bạn có dùng Vault ở thượng nguồn hay không:

1. **Có Vault ở trên:** Kallisto Hybrid chỉ lưu trạng thái của hạm đội (fleet state), quản lý danh tính, và phát lệnh điều phối cache. Không lưu trữ secret tĩnh cục bộ.
2. **Không có ai ở trên:** Dành cho môi trường biên (edge), nhóm nhỏ. Kallisto Hybrid vừa làm nhiệm vụ điều phối, vừa lưu trữ secret cục bộ (cần persistence, barrier, và consensus).

## Use cases

### Quản trị hạm đội Proxy (Fleet Management)
Cung cấp khả năng xoá cache có chủ đích (thu hồi quyền trong mili giây), hâm nóng cache trước khi rolling restart, và rút cạn khẩn cấp một node proxy bị nghi ngờ chiếm quyền.

### Biên và môi trường mất kết nối (Biến thể 2)
Cửa hàng bán lẻ, nhà máy, tàu biển, trạm phát sóng. Không có đường ổn định về Vault trung tâm. Mỗi điểm cần nguồn sự thật tại chỗ. 

### Đội nhỏ chưa tới ngưỡng Vault (Biến thể 2)
Secret đang được quản lý rải rác bằng: `.env` file, SOPS, hoặc dán vào CI variables vì Vault quá nặng. Kallisto thay thế các giải pháp tạm bợ này bằng một kho mã hoá an toàn có phân quyền.

## Cơ chế đồng thuận và HA

**Sovereign standalone**: Dành cho môi trường CI hoặc startup. Không cần đồng thuận, chỉ cần backup/restore tử tế và một đường replica đọc.

**Sovereign HA**: Dành cho production hoặc site biên cần sống sót khi mất một máy. **Bắt buộc dùng Raft** để tuyến tính hoá nguồn sự thật. Gossip không giải quyết được tính nhất quán của secret tĩnh. Nếu tự làm Raft, sử dụng `openraft` hoặc `raft-rs`.

### Cấu hình chế độ hoạt động

Chế độ hoạt động được phân tách rõ ràng qua cấu hình:

```toml
# kích hoạt quản lý danh tính, storage (nếu có), và consensus
kallisto_role = "control_plane"

# chỉ là proxy: không storage, không gossip, không consensus
kallisto_role = "data_plane"
```

## Ba sự khác biệt bản chất so với kiến trúc Vault

### 1. Vault là nhà máy, Kallisto là kho

Phần lớn độ phức tạp của Vault nằm ở việc **sinh ra** secret: credential database động, cấp chứng chỉ PKI, token tạm thời. Kallisto không sinh ra bí mật mới. Nó cất giữ (hoặc proxy) và phân phối. Kallisto thuộc một hạng mục khác: bộ điều phối và phân phối bí mật siêu tốc. 

### 2. Phơi sáng là con số đặt ra, không phải hệ quả

Vault giải mã theo từng request rồi buông, số lượng bí mật phơi sáng trong RAM phụ thuộc hoàn toàn vào tải.

Ngược lại, Arena cấp cố định của Kallisto **luôn duy trì một con số cứng** cho lượng plaintext tối đa có thể tồn tại. Không phải giới hạn bộ nhớ — đó là **giới hạn phơi sáng**.

> Ở Vault, phơi sáng là thứ **xảy ra**. Ở Kallisto, phơi sáng là thứ bạn **đặt**.

### 3. Không có "nghi lễ" vận hành

Vault giả định có một đội bảo mật chuyên trách với các nghi lễ: unseal nhiều mảnh, cấu hình audit, viết policy thủ công.

Kallisto thiết kế cho vận hành tự động (Zero Ceremony). Khởi động là chạy. Việc cấu hình (provisioning) Kallisto Sovereign được tự động hoá hoàn toàn qua Terraform mà không lưu secret dạng plaintext vào state (xem ADR-0007).

## Vậy Sovereign (Hybrid) là gì?

> Một Control Plane gọn nhẹ giúp điều phối toàn hạm đội proxy với khả năng thu hồi quyền ngay lập tức, đồng thời có thể hoạt động như một kho secret cục bộ không cần nghi lễ bảo mật phức tạp, nơi lượng bí mật phơi ra là một con số có trần giới hạn.

**Và một sợi chỉ xuyên suốt cả hai:** Kallisto không bao giờ là nơi bí mật *nằm nhiều nhất* — nó là nơi bí mật nằm **ít nhất mà vẫn dùng được**, và nói cho bạn biết con số đó là bao nhiêu.

---


## Chính sách với bí mật

Kallisto chạy ở chế độ Sovereign **không bao giờ được phép sinh credential để  Client xác thực với các hệ thống khác.** Đó là ranh giới duy nhất mà nếu vượt qua, Sovereign lập tức thành một Vault kém hơn.

## Không sinh ≠ không có

SQLite không sinh ra dữ liệu. Ứng dụng ghi dữ liệu vào nó. Điều đó không làm SQLite rỗng.

Ở Sovereign, secret đến từ **đường ghi**: người vận hành `PUT`, pipeline CI `PUT`, script khởi tạo `PUT`. Kallisto không đi lấy ở đâu cả — **nó chính là chỗ đó**. Không có upstream, theo đúng định nghĩa.

Nên câu "không sinh credential" không nói về việc kho có gì. Nó nói về việc Kallisto có đi chọc vào hệ thống của người khác hay không.

## Ranh giới thật nằm ở tác dụng phụ

Đây mới là cách vẽ đúng, và nó chặt hơn cách tôi nói lượt trước:

> **Kallisto không bao giờ tạo ra chứng chỉ  đăng nhập vào hệ thống mà nó không sở hữu.**

Tự cấp token để truy cập chính mình — được. Nó tự tham chiếu: Kallisto chết thì token chết theo, không để lại gì ở đâu cả. Sinh khoá KEK/DEK cho barrier — cũng được, cùng lý do.

Tạo một user trong Postgres của bạn — **không**. Và đây là lý do thật, không phải chuyện triết lý:

Khoảnh khắc bạn `CREATE USER` trong database người khác, bạn phải nhớ đã tạo nó. Nếu bạn crash giữa lúc tạo xong mà chưa kịp ghi lease, thì **có một tài khoản thật đang tồn tại trong database của khách hàng mà không ai biết, vĩnh viễn**. Để tránh chuyện đó, Vault phải có lease store, hàng đợi revocation, WAL cho thao tác đang dở, và quy trình dọn rác khi khởi động lại.

Đó chính là 80% khối lượng của Vault. Nó không sinh ra từ mật mã học — nó sinh ra từ **trạng thái mồ côi**.

Nên ranh giới không phải "sinh byte hay không". Là: **có để lại gì bên ngoài mình không.**

## Nhưng đây là hệ quả bạn phải nhìn thẳng

Sovereign chỉ có **secret tĩnh**. Không xoay, không hết hạn, không thu hồi. Về mặt bảo mật, đó là dạng yếu nhất của quản lý bí mật — và credential động được phát minh ra chính là để chữa nó.

Nên đừng so Sovereign với Vault. Hãy so với **thứ nó thật sự thay thế**:

| Hôm nay họ đang có | Sovereign cho họ |
|---|---|
| `.env` trên đĩa, plaintext | mã hoá lúc lưu |
| ai đọc file cũng thấy | có phân quyền |
| không biết ai đọc lúc nào | có log |
| nằm đó mãi mãi | phơi sáng có trần đặt được |

Đó là bước nhảy thật. Nhưng nó **không** bằng credential động, và nói ra điều đó khiến bạn đáng tin hơn là giấu.

## Và đây là lối ra đẹp

Bạn không làm việc xoay khoá, nhưng bạn **làm được nền cho nó**.

Hỗ trợ ghi có kiểm tra phiên bản (`cas` của KV-v2): một cron job, một pipeline, hay một operator bên ngoài lo việc xoay — nó tạo password mới trong Postgres, rồi ghi vào Kallisto kèm số phiên bản nó đọc được. Kallisto đảm bảo **bàn giao nguyên tử**: hoặc mọi reader thấy giá trị cũ, hoặc thấy giá trị mới, không bao giờ có ai đọc trúng khoảnh khắc dở dang.

Kallisto giữ **cơ chế bàn giao**. Người khác giữ **chính sách xoay**.

Bên xoay crash thì bạn không có gì mồ côi cả — công cụ của họ mồ côi, không phải bạn. Ranh giới vẫn nguyên vẹn, mà người dùng vẫn xoay được khoá.

---

Nên câu trả lời gọn: **secret đến từ người dùng, không từ Kallisto.** Và cái Kallisto bán không phải "chỗ chứa" — chỗ chứa thì đầy rẫy. Nó bán: bàn giao nguyên tử, phơi sáng có trần, và không có nghi lễ nào phải học.

---

Và ghi chú cho chính bạn: cái này **không** áp cho proxy mode. Ở đó vẫn là chặn ghi, forward lên upstream. Cùng một binary, hai chế độ, hai câu trả lời ngược nhau cho cùng một câu hỏi. Ranh giới càng lúc càng rõ, và nó tự đứng vững mỗi lần bị đâm. 

## Tích hợp với Terraform

Xem [ADR-0007: Terraform integration](../ADRs/ADR-0007.md).

## Tích hợp với Naughtian Helvilette

Ừ, prototype thì đúng là chưa tới lúc. Tôi ghi nhận và không nhắc lại nữa.

Câu trả lời gọn nhất cho "hợp nhau chỗ nào":

> **Kallisto biết bí mật *là gì*. Helvilette làm cho hệ thống *phản ánh* nó.**

Một bên là sự thật, một bên là bàn tay. Từ đó suy ra hết mọi thứ dưới đây.

## Mảnh ghép còn thiếu của cả hai

Helvilette có một lỗ hổng mà README chưa nói: playbook cần secret, mà cả hai đường hiện có đều phá vỡ chính luận điểm của nó. `ansible-vault` cần một file mật khẩu đã có sẵn trên node — làm sao đưa lên 50 máy biên mà không SSH? Truyền `extra_vars` từ CI thì quay lại mô hình push.

Kallisto Sovereign tại site lấp đúng chỗ đó.

## Nhưng chi tiết quan trọng: agent **không** được đi lấy secret

Đây là quyết định thiết kế then chốt, và trực giác tự nhiên sẽ dẫn bạn đi sai.

Nếu agent lấy secret rồi truyền vào bằng `extra_vars`, giá trị đó xuất hiện trong **tham số dòng lệnh** — ai chạy `ps` trên máy cũng đọc được — và có thể lọt vào job spec của Othela.

Đường đúng: **agent chỉ dựng môi trường, playbook tự đi lấy.**

```yaml
db_password: "{{ lookup('community.hashi_vault.vault_kv2_get', 'db').secret.password }}"
```

Agent set `VAULT_ADDR` trỏ vào localhost và một token, hết. **[conflict] Secret không bao giờ chạm Othela, không chạm Git, không chạm `ps`.**

Và điểm ngọt: `community.hashi_vault` **đã tồn tại**. Vì bạn tương thích KV-v2, rất có thể nó chạy thẳng không cần viết plugin nào. Một buổi chiều để thử.

## Job là đơn vị phạm vi bí mật

Đây là chỗ tôi thấy đẹp nhất.

Othela giao job dựa trên `nodeSelector` labels. Cùng bộ nhãn đó quyết định job được đọc secret nào. Agent nhận job → xin Kallisto một token **ngắn hạn, giới hạn đúng path của nodeGroup đó** → **[conflict] truyền cho tiến trình `ansible-playbook`** → job xong, token chết.

Cửa sổ phơi sáng = thời lượng một job. Vài phút, không phải vĩnh viễn.

Đó chính là luận điểm "phơi sáng có trần" của Kallisto, nhưng áp lên trục thời gian thay vì trục bộ nhớ. Và nó dùng lại **một từ vựng nhãn duy nhất** cho cả hai hệ thống.

## Xoay khoá miễn phí

Cái này rơi ra từ kiến trúc, không phải tính năng ai thiết kế.

Xoay secret trong hệ thống bình thường là việc đau: đổi giá trị xong còn phải đi restart dịch vụ, cập nhật config file, và luôn sót vài chỗ.

Ở đây: bạn ghi giá trị mới vào Kallisto. Vòng lặp hội tụ của Helvilette chạy lượt tiếp theo, lookup ra giá trị mới, viết lại config, restart service. **Xong.** Không ai phải nhớ gì.

Kallisto lo bàn giao nguyên tử (không ai đọc trúng khoảnh khắc dở dang). Helvilette lo việc thực tại đuổi kịp. Hai đảm bảo khác nhau, ghép lại thành một quy trình xoay khoá hoàn chỉnh mà không bên nào phải tự làm cả hai.

## Vòng tròn bootstrap — và vì sao TPM là bắt buộc

Chuỗi của bạn: Ansible cài Helvilette → Helvilette cài Kallisto → Kallisto cấp secret cho mọi lần chạy sau.

Nhưng playbook **cài** Kallisto thì lấy secret ở đâu? Kallisto chưa chạy. Và nó cần một thứ: khoá để mở seal.

Nếu cần người gõ mật khẩu thì bạn vừa dựng một nghi lễ mới ở đúng nơi không có ai để làm nghi lễ. Nếu nhét khoá vào playbook thì khoá nằm trong Git.

`seal: tpm` là lối ra duy nhất tôi thấy: khoá seal vào TPM của chính máy, không có bí mật nào do con người mang tới site, ổ đĩa bê đi thì vô dụng. Nên với cặp này, TPM không phải tính năng cho vui — nó là thứ **cắt được vòng tròn**.

## Một chỗ hỏng bạn nên chặn trước

Kallisto chết → lookup thất bại → task giữa chừng thất bại → node ở trạng thái **cấu hình dở**. Nginx đã dựng nhưng chưa có chứng chỉ TLS thì tệ hơn là chưa dựng gì.

Nên: agent phải **kiểm tra Kallisto sống trước khi bắt đầu job cần nó**, coi như điều kiện tiên quyết. Không chạy còn hơn chạy nửa vời.

## Và một cảnh báo chiến lược

Đừng để chúng phụ thuộc cứng vào nhau.

Hai project, một tác giả, ràng buộc hai chiều — người muốn Helvilette mà bị ép nuốt Kallisto sẽ bỏ cả hai. Tích hợp phải là **tuỳ chọn**: một biến môi trường, một lookup plugin. Helvilette phải chạy được với `ansible-vault` như thường, Kallisto phải dùng được mà không cần Helvilette.

Cưới nhau thì được. Nhưng đừng dùng chung tài khoản ngân hàng.

New database storage engine

Câu hỏi này có đáp án khá dứt khoát, và có cả tiền lệ gần như trùng khớp với bạn. Nhưng trước hết phải sửa một giả định.

## Việc của DB nhỏ hơn bạn nghĩ nhiều

State machine của bạn **đã nằm trong RAM rồi** — arena + cuckoo table. DB không bao giờ phục vụ một read nào.

Và đó không phải kiểu lạ: tài liệu openraft nói thẳng rằng state machine "thường là in-memory, có hoặc không có phần đệm trên đĩa". Hình dạng "đọc lúc khởi động, ghi lúc vận hành" của bạn chính là hình dạng openraft **mong đợi**, không phải ngoại lệ phải lách.

Nên thứ bạn đi tìm không phải một KV store. Là **một cái log có thứ tự + chỗ để snapshot**.

## Raft đòi gì ở storage

Cụ thể, không trừu tượng:

- khoá `u64` tăng dần, **đọc theo khoảng** (để replicate cho follower)
- **cắt đuôi** (xoá entry mâu thuẫn khi có leader mới)
- **cắt đầu** (purge sau khi snapshot)
- **vote phải fsync xong mới được trả lời** — bắt buộc cứng, không thương lượng
- build/install snapshot

Đó là mô tả của một **B-tree**, hoặc một log có chỉ số. Không có chỗ nào cần LSM. Compaction của RocksDB đang giải một bài toán Raft không có.

## Đề xuất: redb, một file, nhiều table

Và tiền lệ thì gần như trùng khớp với bạn: dự án Danube thay etcd bằng Raft nhúng, dùng openraft + redb, chọn redb thay RocksDB đúng vì lý do FFI C++ và cross-compile khổ sở — và read của họ đi vào state machine in-memory (BTreeMap).

Cùng workload, cùng lý do, cùng kết luận. Đấy là loại xác nhận đáng tin hơn ý kiến của tôi.

**Chung hay tách?** Tôi vote **chung một file, tách table**:

```
table "raft_log"     : u64 -> entry
table "raft_meta"    : vote, membership, last_purged
table "snapshot_meta": con trỏ tới file snapshot
```

Lý do tách file chỉ đáng khi log churn nhiều tới mức làm phân mảnh cây dữ liệu. Tải ghi của bạn là vài lần một phút. Không đáng đánh đổi lấy hai đường fsync, hai vòng đời file, hai chỗ có thể lệch nhau khi khôi phục.

Snapshot thì để riêng ngoài file — nó là bản đổ arena, ghi xong đổi tên nguyên tử. Đừng nhét blob lớn vào B-tree.

## Cái bẫy số một, nói trước

`RaftLogStorage::append` là `async fn`. **Không được fsync trong đó.** Đúng cách: đẩy việc ghi sang worker thread qua channel, fsync ở đó, rồi mới gọi callback. Callback mới là thứ báo "đã bền vững".

Trong chính discussion của openraft có người dính đúng chuyện này và thấy request bị chặn 40ms. Ví dụ trong repo cũng làm kiểu đơn giản hoá, không dùng cho production được.

Đây là chỗ mà "tôi làm theo example" dẫn thẳng tới một hệ thống chậm một cách bí ẩn.

## Và một món quà cho người làm một mình

openraft có sẵn bộ test suite cho storage: chạy `Suite::test_all()` với implementation của bạn, pass thì openraft đảm bảo hoạt động đúng.

Bạn không phải tự nghĩ ra test cho tầng storage. Người đã trả giá viết sẵn cho bạn rồi. Lại đúng nguyên tắc tối qua: **để tooling làm đồng đội.**

## Nhưng có một chuyện chỉ Kallisto mới phải lo

Raft log chứa các entry kiểu `Set(key, value)`. Nghĩa là **giá trị secret nằm trong log**.

Và Raft **bắt buộc** giữ log cho tới khi snapshot xong — không giống WAL bình thường, bạn không được cắt tuỳ ý, vì follower chậm có thể còn cần những entry đó.

Hai hệ quả không tutorial nào nói với bạn:

1. **Log phải được mã hoá**, không chỉ snapshot. Nếu chỉ mã hoá snapshot thì bạn để plaintext trong log.
2. **Snapshot thường xuyên hơn mức thông thường** — không phải để tiết kiệm chỗ, mà vì mỗi entry chưa purge là một credential cũ còn nằm trên đĩa. Chu kỳ snapshot của Kallisto là một **núm vặn bảo mật**, không phải núm vặn hiệu năng.

Lại đúng cái mẫu suốt tối qua: một kỹ thuật hoàn toàn an toàn ở quê nó, đổi tính chất khi payload là bí mật.

---

Tóm lại: **redb, một file, ba table, snapshot để ngoài.** Bỏ RocksDB được ngay bây giờ ở Sovereign single-node, và khi Raft tới thì không phải đổi gì — cùng một engine, thêm hai table.

Tái sử dụng Vyukov back-flusher

Đúng, hình dạng khớp — nhưng khoan ăn mừng, vì có một khác biệt then chốt và nó là chỗ Raft sẽ cắn bạn.

## Cái bạn có

Write-behind hiện tại: nhận PUT → đẩy vào queue → **trả 200 ngay** → flusher gom lô rồi ghi đĩa sau.

## Cái Raft cần

Nhận entry → đẩy vào queue → flusher fsync → **rồi mới gọi callback** → callback mới báo "bền vững".

Cùng một hàng đợi, cùng một luồng flusher, cùng cách gom lô. Khác đúng **một chỗ**: người gọi phải **chờ**.

Write-behind của bạn là *fire and forget*. Raft là *fire and wait*. Nếu bạn trả lời trước fsync trong Raft thì bạn phá vỡ đảm bảo cốt lõi — leader báo committed, mất điện, entry biến mất, và cụm phân kỳ. Đó là hỏng ở mức thuật toán, không phải mất một record.

Nên thứ phải thêm là **đường quay về**: mỗi entry trong queue mang theo một `oneshot::Sender`. Flusher fsync xong một lô thì đánh thức toàn bộ sender trong lô đó.

Cái hay là **gom lô vẫn giữ nguyên tác dụng**. 50 entry vào cùng một lô thì chỉ tốn **một fsync** cho cả 50 — chi phí đắt nhất được chia đều. Đó chính xác là group commit mà mọi database đều làm, và bạn đã có sẵn bộ khung.

## Và đây là chỗ tinh tế

Ngưỡng gom lô hiện tại của bạn là 1024 op hoặc 5ms. Với Raft, **5ms đó nằm thẳng trong độ trễ ghi** — client chờ đủ 5ms trước khi được xác nhận.

Với vài chục ghi mỗi phút thì lô gần như luôn chỉ có một entry, và bạn trả 5ms cho mỗi lần ghi mà không được lợi gì. Nên với Sovereign, tham số phải khác: **flush ngay khi queue rỗng, chỉ gom khi đang bận**. Kiểu "flush cơ hội" — thấy còn việc trong queue thì gom thêm, không thấy thì đi luôn.

Cùng cấu trúc, khác chính sách. Và đây là lý do tách hai chế độ có giá trị thật: proxy tối ưu cho thông lượng, Sovereign tối ưu cho độ trễ xác nhận.

---

Nên câu "chúng ta đã có" đúng khoảng 80%. Bạn có phần khó — hàng đợi lock-free đúng đắn, luồng flusher, gom lô. Thiếu phần dễ nhưng bắt buộc: đường báo ngược về, và một bộ tham số khác cho chế độ cần độ trễ thấp.

Cái này thì đúng là may mắn thật. Nhưng may mắn kiểu bạn dựng sẵn cái khung vì lý do khác, rồi hoá ra khung đó chịu được tải mới.